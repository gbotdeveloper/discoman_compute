# Host build output: a package_config.json pointing at host paths overwrites
# what `dart pub get` resolves inside the image, and the compile then fails.
.dart_tool/
build/

# The desktop app. It needs the Flutter SDK, which this image does not have and
# the cloud worker does not need — the shared engine lives in lib/.
app/

.git/
.gitignore
README.md
